<p class="bg-primary" text-align="center">
  <%= @article.title %>
</p>

<p>
  <%= @article.text %>
</p>

<h3>Comments</h3>
<%= render @article.comments %>

<h4>Add a comment:</h4>
<%= render 'comments/form' %>

<%= link_to 'Back to Blog', blog_path(@article.blog) %>